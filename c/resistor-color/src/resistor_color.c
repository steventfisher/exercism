#include "resistor_color.h"
#include <stdint.h> //needed for uint16

uint16_t colorCode(resistor_band_t colors[]){

  resistor_band_t bands[] = {BLACK, BROWN, RED, ORANGE, YELLOW,
			     GREEN, BLUE, VIOLET, GREY, WHITE};
  
  return 0;
}
